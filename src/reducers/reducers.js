
const initialState = {
    name: 'Nate'
}

function pitchCounter ( state, action ) {
    if ( typeof state === 'undefined' ){
        return initialState;
    }

    // TODO: Implement
    
    return state;
}